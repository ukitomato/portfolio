import React from 'react';
import { shallow } from 'enzyme';
import ColoredLine from '../../components/ColoredLine';

it('should render without crashing', () => {
    shallow(<ColoredLine color={'white'} className={'test'} />);
});

it('should render without crashing', () => {
    shallow(<ColoredLine color={'white'} className={'test'} />);
});
